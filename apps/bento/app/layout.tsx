import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://yourdomain.com"), // 🔥 Replace with real domain

  title: {
    default: "ARGUS Intelligence",
    template: "%s | ARGUS Intelligence",
  },

  description:
    "Commercial real estate intelligence platform for portfolio optimization and analytics.",

  openGraph: {
    title: "ARGUS Intelligence",
    description:
      "Commercial real estate intelligence platform for portfolio optimization and analytics.",
    type: "website",
  },

  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
